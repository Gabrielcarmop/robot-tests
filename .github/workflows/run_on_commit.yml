name: Testes com Login + Gemini + GitHub

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 8 * * *'
    - cron: '0 20 * * *'

jobs:
  executar-testes:
    runs-on: ubuntu-latest
    
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      MY_GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
      GITHUB_REPO: ${{ github.repository }}
    
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3
      
      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Instalar dependências Python
        run: |
          pip install --upgrade pip
          pip install robotframework
          pip install robotframework-seleniumlibrary
          pip install robotframework-requests
          pip install requests
          pip install allure-robotframework
      
      - name: Instalar ChromeDriver compatível automaticamente
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip google-chrome-stable
          
          # Detecta versão instalada do Chrome
          CHROME_VERSION=$(google-chrome --version | grep -oP '\d+\.\d+\.\d+\.\d+')
          CHROME_MAJOR=$(echo $CHROME_VERSION | cut -d '.' -f 1)
          echo "Versão completa do Chrome: $CHROME_VERSION"
          echo "Versão major do Chrome: $CHROME_MAJOR"
          
          # Busca a versão mais recente disponível do ChromeDriver para este major
          LATEST_VERSION=$(curl -s "https://googlechromelabs.github.io/chrome-for-testing/latest-versions-per-milestone-with-downloads.json" | \
            grep -A 50 "\"milestone\": \"$CHROME_MAJOR\"" | \
            grep -m 1 '"version"' | \
            grep -oP '\d+\.\d+\.\d+\.\d+')
          
          echo "Versão do ChromeDriver a ser baixada: $LATEST_VERSION"
          
          # Monta URL do ChromeDriver
          DRIVER_URL="https://storage.googleapis.com/chrome-for-testing-public/${LATEST_VERSION}/linux64/chromedriver-linux64.zip"
          echo "URL do ChromeDriver: $DRIVER_URL"
          
          # Baixa e instala
          wget -O chromedriver.zip "$DRIVER_URL"
          unzip -o chromedriver.zip
          sudo mv chromedriver-linux64/chromedriver /usr/local/bin/
          sudo chmod +x /usr/local/bin/chromedriver
          
          # Limpa arquivos temporários
          rm -rf chromedriver.zip chromedriver-linux64
      
      - name: Validar versões
        run: |
          echo "=== Versões Instaladas ==="
          google-chrome --version
          chromedriver --version
          echo "=========================="
      
      - name: Criar arquivo de teste
        run: |
          mkdir -p resources
          echo "Arquivo de teste para upload - $(date)" > resources/teste.txt
      
      - name: Executar testes com Robot Framework + Allure
        run: |
          mkdir -p results
          xvfb-run robot --outputdir results --listener allure_robotframework tests/
      
      - name: Instalar Allure CLI
        if: always()
        run: |
          wget https://github.com/allure-framework/allure2/releases/download/2.27.0/allure-2.27.0.tgz
          tar -zxvf allure-2.27.0.tgz
          sudo mv allure-2.27.0 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/bin/allure
          allure --version
      
      - name: Gerar relatório Allure
        if: always()
        run: |
          allure generate results -o allure-report --clean
      
      - name: Upload do relatório Allure
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report
      
      - name: Fazer upload dos relatórios e screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: relatorio-testes
          path: results/